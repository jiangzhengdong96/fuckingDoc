### 线程问题集

###### 目录

- [自测问题](#自测问题)
- [易错问题](#易错问题)
- [待验证](#待验证)

<a id="自测问题"></a>
###### 自测问题

- `start()` 和 `run()` 有什么区别？
- Java 线程状态如何流转？
- `sleep()` 和 `wait()` 有什么区别？
- `volatile` 解决什么问题？
- `synchronized` 锁对象怎么判断？

<a id="易错问题"></a>
###### 易错问题

- `volatile` 能不能保证 `i++` 安全？
- `wait()` 能不能在 synchronized 外调用？
- Android 子线程能不能直接更新 UI？
- `Thread.stop()` 为什么不推荐？

<a id="待验证"></a>
###### 待验证

- Java 线程状态和操作系统线程状态的关系。
- synchronized 锁升级细节。
- 线程池参数和拒绝策略单独拆分。
