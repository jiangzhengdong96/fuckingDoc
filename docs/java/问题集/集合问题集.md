### 集合问题集

###### 目录

- [自测问题](#自测问题)
- [源码问题](#源码问题)
- [待验证](#待验证)

###### 自测问题

- `List`、`Set`、`Map` 的核心区别是什么？
- `ArrayList` 为什么随机访问快？
- `HashSet` 为什么需要元素实现 `equals` 和 `hashCode`？
- `HashMap` 为什么不是线程安全的？

###### 源码问题

- `HashMap.put()` 的主流程是什么？
- `HashMap` 什么时候扩容？
- `HashMap` 什么时候树化？
- `ConcurrentHashMap` 在不同 JDK 版本中的并发控制有什么差异？

###### 待验证

- JDK8 `HashMap` 树化和退化阈值。
- `ArrayList` 扩容倍率和源码实现。
- `LinkedHashMap` 如何维护访问顺序。
