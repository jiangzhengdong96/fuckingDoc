### 集合问题集

###### 目录

- [自测问题](#自测问题)
- [源码问题](#源码问题)
- [待验证](#待验证)

<a id="自测问题"></a>
###### 自测问题

- `List`、`Set`、`Map` 的核心区别是什么？
- `ArrayList` 为什么随机访问快？
- `HashSet` 为什么需要元素实现 `equals` 和 `hashCode`？
- `HashMap` 为什么不是线程安全的？
- `Arrays.asList(new int[]{1, 2, 3})` 得到的 List 长度是多少？
- `subList()` 修改后原 List 会不会变？
- `HashMap` 的 key 如果修改了参与 `hashCode()` 的字段，还能不能正常查到？
- `TreeSet` 的比较器和 `equals()` 不一致时，元素去重按谁算？
- `Collections.unmodifiableList()` 包装后，如果原 List 修改了，视图会不会变化？
- `ConcurrentHashMap` 为什么不允许 `null` key 和 `null` value？
- Android 里 `SparseArray` 和 `HashMap<Integer, T>` 的使用场景有什么区别？

<a id="源码问题"></a>
###### 源码问题

- `HashMap.put()` 的主流程是什么？
- `HashMap` 什么时候扩容？
- `HashMap` 什么时候树化？
- `ConcurrentHashMap` 在不同 JDK 版本中的并发控制有什么差异？

<a id="待验证"></a>
###### 待验证

- JDK8 `HashMap` 树化和退化阈值。
- `ArrayList` 扩容倍率和源码实现。
- `LinkedHashMap` 如何维护访问顺序。
- `CopyOnWriteArrayList` 写入时为什么成本高。
- `BlockingQueue` 常见实现类和使用场景。
- `SparseArray`、`ArrayMap` 在 Android 不同版本和数据规模下的性能差异。
