### Java 基础知识点清单

###### 内容概述

本文件记录 Java 基础范畴的知识点，包括基础语法、修饰符、变量、源文件声明规则、数据类型、运算符、包装器类、Number、数组、正则表达式、代码块、可变参数、主方法、递归、垃圾回收、深浅拷贝和值传递。

###### 使用场景

- Android 项目中阅读 Java SDK、Android Framework、三方库源码时需要这些基础语法。
- 修饰符、变量、常量、代码块常见于工具类、单例、SDK API 和对象初始化逻辑。
- 数据类型、包装器类、数组和值传递常用于接口参数、数据模型和基础算法处理。
- GC、深浅拷贝和值传递有助于理解内存问题、对象修改和参数传递行为。

一级栏目导航：

- [基础语法](#基础语法)
- [修饰符](#修饰符)
- [变量](#变量)
- [源文件声明规则](#源文件声明规则)
- [Java 的两大数据类型](#java-的两大数据类型)
- [运算符](#运算符)
- [instanceof 运算符](#instanceof-运算符)
- [包装器类](#包装器类)
- [Number](#number)
- [数组](#数组)
- [正则表达式](#正则表达式)
- [代码块（普通代码块、静态代码块、构造代码块、同步代码块）](#代码块（普通代码块、静态代码块、构造代码块、同步代码块）)
- [可变参数](#可变参数)
- [主方法（main 方法）](#主方法（main-方法）)
- [递归](#递归)
- [垃圾回收](#垃圾回收)
- [深拷贝和浅拷贝](#深拷贝和浅拷贝)
- [值传递和引用传递](#值传递和引用传递)
- [面试可能怎么问](#面试可能怎么问)

<a id="基础语法"></a>
###### 基础语法

**大小写敏感：**Java 是大小写敏感的，这就意味着标识符 Hello 与 hello 是不同的。

**类名：**对于所有的类来说，类名的首字母应该大写。如果类名由若干单词组成，那么每个单词的首字母应该大写，例如 MyFirstJavaClass 。

**方法名：**所有的方法名都应该以小写字母开头。如果方法名含有若干单词，则后面的每个单词首字母大写。

**常量名：**基本数据类型的常量名使用全部大写字母，字与字之间用下划线分隔。对象常量可大小混写。例如，SIZE_NAME。

在 Java 中使用 final 关键字来修饰常量，声明方式和变量类似：

Java 所有的组成部分都需要名字。类名、变量名以及方法名都被称为标识符。

所有标识符首位都以azAZ$_开始，首位以后可以加数字，实际编码不建议用 $，它常见于编译器生成类名

<a id="修饰符"></a>
###### 修饰符

**访问控制修饰符 :** default, public , protected, private

protected, private 俩不能修饰类，只能修饰变量和方法

default：仅同包可见（“包级私有”，无修饰符）

protected：同包 + 子类可见（“受保护的”），通常只能通过当前子类对象或子类引用访问继承来的成员

**非访问控制修饰符 :** final, abstract, static, synchronized

静态方法不能使用类的非静态变量，访问静态方法直接类名.静态方法，类加载过程中时就被[装载和分配（待定）]()，非静态方法必须在类被实例化后才能调用

static 变量属于类，不属于某个对象；多个对象共享同一份静态变量

static final 不等于编译期常量。只有“基本类型/String + 编译期可算出来的初始化值”，才是编译期常量。

final 表示"最后的、最终的"含义，变量一旦赋值后，不能被重新赋值。被 final 修饰的实例变量必须显式指定初始值。修饰基本类型表示值不能变；修饰引用类型表示引用不能重新指向，但对象内部状态可能还能变

final 修饰符通常和 static 修饰符一起使用来创建类常量。

final 类不能被继承，没有类能够继承 final 类的任何特性。

final修饰方法能够防止方法被重写，还有明确标识的作用，还有起到优化作用（[jvm内联优化]()）

抽象方法不能被声明成 final 和 static。

任何继承抽象类的子类必须实现父类的所有抽象方法，除非该子类也是抽象类。

如果一个类包含若干个抽象方法，那么该类必须声明为抽象类。抽象类可以不包含抽象方法。

<a id="变量"></a>
###### 变量

变量就是申请内存来存储值。也就是说，当创建变量的时候，需要在内存中申请空间。内存管理系统根据变量的类型为变量分配存储空间，分配的空间只能用来储存该类型数据。

**局部变量**：在方法、构造方法或者语句块中定义的变量被称为局部变量。变量声明和初始化都是在方法中（调用时），方法结束后，变量就会自动销毁，不区分静态方法和普通方法。

访问修饰符不能用于局部变量；

局部变量是在[栈帧（虚拟机栈]()）上分配的。

不能用static修饰

 局部变量没有默认值，所以局部变量被声明后，必须经过初始化，才可以使用。

**成员变量（实例变量）**：成员变量是定义在类中，方法体之外的变量。这种变量在创建对象的时候实例化。成员变量可以被类中方法、构造方法和特定类的语句块访问。

访问修饰符可以修饰实例变量；

创建被分配在[堆内存]()

实例变量具有默认值。数值型变量的默认值是0，布尔型变量的默认值是false，引用类型变量的默认值是null。变量的值可以在声明时指定，也可以在构造方法中或者方法中指定；

可通过object.变量名访问

**类变量（静态变量）**：类变量也声明在类中，方法体之外，但必须声明为static类型。

静态变量在类或者类成员被第一次访问时（

静态常量-常量池、 子类调用父类的静态变量时除外），类被加载，静态变量也会被创建，在类被卸载时销毁。

无论一个类创建了多少个对象，类只拥有类变量的一份拷贝

静态变量储存在[静态存储区（不同版本存在不同地方，jdk8是堆内存了，默认为堆内存]()）。经常被声明为常量，很少单独使用static声明变量。

默认值和实例变量一样，可见也和实例变量一样，但大多数声明为public，主要用来初始化常量

可通过class.变量名访问

public static final后变量名通常大写

避免静态变量持有大对象的长期引用，不然可能堆内存溢出

<a id="源文件声明规则"></a>
###### 源文件声明规则

一个源文件中只能有一个public类

一个源文件可以有多个非public类

源文件的名称应该和public类的类名保持一致。例如：源文件中public类的类名是Employee，那么源文件应该命名为Employee.java。

<a id="java-的两大数据类型"></a>
###### Java 的两大数据类型

**基本数据类型：**

byte 数据类型是8位、有符号的，以二进制补码表示的整数；

char类型是一个单一的 16 位 Unicode 字符；不等于“一个完整字符”。emoji、部分生僻字可能需要两个 char 表示

short 数据类型是 16 位、有符号的以二进制补码表示的整数

int 数据类型是32位、有符号的以二进制补码表示的整数；

long 数据类型是 64 位、有符号的以二进制补码表示的整数；

float 数据类型是单精度、32位、符合IEEE 754标准的浮点数；

double 数据类型是 64 位、浮点数；

boolean数据类型表示一位的信息；

注意：

byte、short、char 参与算术运算时通常会提升为 int

浮点数不适合金额计算，金额建议用 BigDecimal，而且优先用字符串构造：new BigDecimal("0.1")

**引用数据类型：**

字符串、数组、自定义类、java自带类都是引用数据类型。所有引用类型的默认值都是null。



**数据类型：**自动转换级：byte → short → int → long → float → double（char类型特殊，可自动转换为int、long等数值类型）

强制类型转换是由高级到低级，浮点数到整数直接去掉的小数，而不是四舍五入

<a id="运算符"></a>
###### 运算符

Java定义了位运算符，应用于整数类型(int)，长整型(long)，短整型(short)，字符型(char)，和字节型(byte)等类型。有&与运算、|或运算、^异或运算、~非运算、<<左移、 >>右移等，都是转成二进制然后运算，返回值是数值’  （无短路）

逻辑运算符&&、||、! （有短路）

<a id="instanceof-运算符"></a>
###### instanceof 运算符

该运算符用于操作对象实例，检查该对象是否是一个特定类型（类类型或接口类型），就是判断该属性或对象的类型是否正确，返回布尔值

( Object reference variable ) instanceof  (class/interface type)

**优先级：**括号>一元（++，--，！，~）>乘法>加减>位移（>>）>关系（>、<）>相等>位运算>逻辑运算>条件>赋值



<a id="包装器类"></a>
###### 包装器类

自动装箱是基本类型转包装类，自动拆箱是包装类转基本类型

包装类是不可变对象，因为内部值是private final修饰的，修改的话是创建了一个新的对象

**包装类缓存和“常量池”不是一回事**，Integer 这种包装类的缓存，不是 JVM 的“常量池”，只是包装类自己维护的一批复用对象，

Integer a = 127;
Integer b = 127;

System.out.println(a == b);  true
这里 true 不是因为 127 在常量池里，而是因为自动装箱调用了Integer.valueOf(127)，它对 -128 ~ 127 默认会复用缓存对象，如果是128的话就会返回false，因为128 默认不在 IntegerCache 范围内，所以可能创建两个不同对象。

原理就是这个原理，默认缓存对照：
Byte：全部缓存，因为 byte 范围就是 -128 ~ 127
Short：默认缓存 -128 ~ 127
Integer：默认缓存 -128 ~ 127
Long：默认缓存 -128 ~ 127
Character：默认缓存 0 ~ 127
Boolean：缓存 TRUE / FALSE 两个对象

byte、short、char 参与算术运算时，都会先提升为 int

<a id="number"></a>
###### Number

所有的包装类**（Integer、Long、Byte、Double、Float、Short）**都是抽象类 Number 的子类。

<a id="数组"></a>
###### 数组

数组是对象，长度固定，length 是字段不是方法。

数组元素有默认值：数字是 0，boolean 是 false，引用是 null。

数组支持协变：Object[] arr = new String[1] 可以编译，但放入非 String 会 ArrayStoreException。

静态创建-> int\[] arrayRefVar = {1,2}

动态创建-> int\[] arrayRefVar = new int\[5];



二维数组(也可以静态和动态创建)：

type\[]\[] typeName = new type\[typeLength1]\[typeLength2];

java.util.Arrays 类能方便地操作数组，它提供的所有方法都是静态的，有排序sort、toString转字符串、数组填充fill、拷贝等。

<a id="正则表达式"></a>
###### 正则表达式

可以使用字符串自带的方法简单匹配：matches、replaceAll、split

也可以用Pattern和Matcher类：

String str = "Java123，Python456，C789"; // 1. 编译正则（规则：匹配1个及以上数字） Pattern pattern = Pattern.compile("\\\d+"); // 2. 创建匹配器，关联目标字符串 Matcher matcher = pattern.matcher(str);

<a id="代码块（普通代码块、静态代码块、构造代码块、同步代码块）"></a>
###### 代码块（普通代码块、静态代码块、构造代码块、同步代码块）

普通方法代码块：方法被调用，并且执行流走到它这里时才执行，不能脱离方法单独存在
静态代码块：类初始化时执行一次，优先于对象创建，不能直接访问非静态成员，常用于初始化静态资源

普通非方法代码块：每次创建对象时，先于构造方法执行，每 new 一次执行一次，在构造方法之前执行，常用于提取多个构造方法共有初始化逻辑
同步代码块：用于线程同步，必须有锁对象，同一把锁同一时间只能被一个线程持有

<a id="可变参数"></a>
###### 可变参数

typeName... parameterName作为方法的参数，前面是类型，后面是参数名，调用一个元素直接parameterName[i]，一个方法最多只能有一个可变参数，必须放在参数列表最后

<a id="主方法（main-方法）"></a>
###### 主方法（main 方法）

程序的“入口”，JVM运行程序时，会优先执行main方法，格式固定为 `public static void main(String[] args) { ... }`，不可随意修改（除了args参数名可自定义）,

可以重载，但程序入口只认标准签名

<a id="递归"></a>
###### 递归

递归最关键看两件事，有没有终止条件，每一层是不是在逼近终止条件

- 每次递归调用都会创建新的栈帧
- 层数太深会抛 StackOverflowError
- Java 一般不做尾递归优化，不能指望编译器帮你消掉栈
- 深递归场景常要改写成循环、显式栈，或加记忆化

<a id="垃圾回收"></a>
###### 垃圾回收

没有被引用的对象会被回收，主要是堆内存中的对象以及方法区常量池里面的部分常量，不可控，开发只需要把引用置为null即可

1. Java 程序员不能直接释放对象，只能让对象变成不可达，等待 GC。
2. 判断对象是否可回收，主流 JVM 使用可达性分析，不是单纯引用计数。
3. GC Roots 可以简单列：虚拟机栈引用、方法区静态属性引用、常量引用、本地方法栈 JNI 引用等。
4. System.gc() 只是建议 JVM 执行 GC，不保证马上执行。
5. finalize() 已经过时，不应该作为资源释放手段。
6. 文件、Socket、数据库连接这类资源不应该等 GC，应该主动 close，优先用 try-with-resources。
7. 强引用、软引用、弱引用、虚引用可以先列入口，详细放 JVM 专

<a id="深拷贝和浅拷贝"></a>
###### 深拷贝和浅拷贝

* 浅拷贝：对基本数据类型是进行值拷贝， 对引用数据类型是进行引用的拷贝
* 深拷贝：对基本数据类型是进行值拷贝， 对引用数据类型是会创建一个新的对象对其内容的复制的拷贝

<a id="值传递和引用传递"></a>
###### 值传递和引用传递

* 值传递， 函数参数传递方式是值的拷贝或者对象引用的拷贝， 不能够直接在函数内部修改值或者对象引用地址， 只能够修改对象引用的状态。 所以java都是值传递
* 引用传递， 函数参数传递的是对象的引用，可以在函数内部修改对象的引用地址， 一般在c++或者Python中才能见到

<a id="面试可能怎么问"></a>
###### 面试可能怎么问

更完整的问答整理见 [java基础面试题.md](../面试/java基础面试题.md)。

- Java 为什么是大小写敏感的？命名规范通常怎么约定？
- Java 标识符可以用哪些字符？为什么实际编码里不建议随便使用 `$`？
- `public`、`protected`、默认访问权限、`private` 有什么区别？
- `protected` 是不是任意子类对象都能访问父类 protected 成员？
- `static` 修饰变量、方法、代码块分别表示什么？
- 静态方法为什么不能直接访问非静态成员？
- `static final` 一定是编译期常量吗？
- `final` 修饰基本类型和引用类型有什么区别？
- `final` 修饰类、方法、变量分别有什么效果？
- 抽象类中可以没有抽象方法吗？有抽象方法的类一定要声明为抽象类吗？
- 抽象方法为什么不能同时用 `final` 或 `static` 修饰？
- 局部变量、成员变量、静态变量有什么区别？
- 局部变量为什么没有默认值？
- 实例变量和静态变量分别存在哪里？生命周期有什么区别？
- 一个 `.java` 源文件中为什么只能有一个 `public` 类？
- 源文件名为什么要和 `public` 类名保持一致？
- Java 有哪些基本数据类型？各自大概占多少位？
- `char` 为什么不一定能表示一个完整字符？
- `byte`、`short`、`char` 参与运算时会发生什么类型提升？
- 自动类型转换和强制类型转换有什么区别？
- 为什么金额计算不推荐使用 `float` 或 `double`？
- `&&` 和 `&`、`||` 和 `|` 有什么区别？
- 位移运算中 `>>` 和 `>>>` 有什么区别？
- `instanceof` 的作用是什么？能判断接口类型吗？
- 什么是自动装箱和自动拆箱？
- `Integer a = 127; Integer b = 127; a == b` 为什么可能是 `true`？
- 包装类缓存和字符串常量池是一回事吗？
- 包装类为什么通常说是不可变对象？
- 数组是不是对象？`length` 是字段还是方法？
- Java 数组协变会带来什么问题？
- `Arrays` 工具类常用方法有哪些？
- Java 正则里 `Pattern` 和 `Matcher` 分别负责什么？
- 静态代码块、构造代码块、普通代码块、同步代码块分别什么时候执行？
- 可变参数的底层本质是什么？为什么必须放在参数列表最后？
- `main` 方法为什么必须是 `public static void main(String[] args)`？
- `main` 方法可以重载吗？程序入口认哪一个？
- 递归需要满足哪些条件？递归过深会出现什么问题？
- Java 会做尾递归优化吗？
- Java 程序员能不能手动释放对象？
- JVM 怎么判断对象是否可以被回收？
- `System.gc()` 能不能保证马上触发 GC？
- 为什么不应该依赖 `finalize()` 释放资源？
- 深拷贝和浅拷贝有什么区别？
- Java 到底是值传递还是引用传递？对象作为参数传递时传的是什么？
